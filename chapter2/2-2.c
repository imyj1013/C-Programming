#include<stdio.h>

int main()
{
	double a = 4.200000;   // 변수 a 선언
	double b = 5.100000;   // 변수 b 선언
	printf("first number: %f\n", a);   // 부동소수형으로 출력
	printf("second number: %f\n", b);  // 부동소수형으로 출력
	double sum = a + b;   // 변수 sum 선언
	printf("sum: %f", sum);    // 부동소수형으로 출력
	return 0;
}
